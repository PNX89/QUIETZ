$ uv run python scripts/measure_incident.py

Two feeds break and keep breaking. 8 evaluations of 3 rules, posted 2 seconds apart,
which is 24 alert firings in total.

  notifications a human receives   2
    oncall           1
    ticket           1

  distinct alerts that reached anybody:
    ecb_reference_rate_freshness
    ecb_yield_curve_freshness

ecb_reference_rate_completeness fired 8 times and reached nobody. It is suppressed
while ecb_reference_rate_freshness is firing on the same feed: a feed that has not
delivered at all cannot be incomplete in any interesting way, so it is the same
problem, restated. The data-platform route it would have travelled did deliver, which
is what makes that absence inhibition rather than silence.

Grouping merged nothing here. No notification carried more than 1 alert, because every
alert that survived inhibition routes to a different receiver.
